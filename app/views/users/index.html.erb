<h1 class="title-page-welcome">List of Users</h1>
  <div class="park-tile-section">
    <ul class="park-tile-list">
    <% @users.each do |user| %>
      <li class="park-tile">
        <%= link_to "#{user.username}", user_path(user.id) %>
      </li>
    <% end %>
  </ul>
  </div>
